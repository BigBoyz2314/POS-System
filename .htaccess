Options -Indexes
RewriteEngine On

# Allow API and Admin to pass through
RewriteCond %{REQUEST_URI} ^/api [NC]
RewriteRule ^ - [L]
RewriteCond %{REQUEST_URI} ^/admin [NC]
RewriteRule ^ - [L]

# Serve existing files
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# SPA fallback for root Shop
RewriteRule ^ index.html [L]

Options -Indexes
RewriteEngine On

# Handle API calls
RewriteCond %{REQUEST_URI} ^/api [NC]
RewriteRule ^ - [L]

# Serve existing files
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# SPA fallback
RewriteRule ^ index.html [L]
